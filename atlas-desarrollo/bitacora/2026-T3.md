
---

## S-12 — 2026-09-19 — Claude Opus 5

- **Carril / pieza / hito o lote / fase o paso:** Software — motor — `v0.1-cerebelo` — fase 3
- **Qué se hizo:**
  - **Repositorio publicado.** 13 commits en `neuroteca/neuroteca.github.io`. Antes del push
    se reescribió la autoría al `noreply` de GitHub que dio Max: **cero rastro de su correo
    personal** en toda la historia. Verificado en la página de commits: atribuidos a
    `MaxAlro` con perfil enlazado. `DEP-03` resuelta.
  - **Pasos 3, 6, 7, 8 y 9 completados.** El sitio se genera y se navega: 13 páginas, 105 KB,
    migas y ejes en HTML puro, URLs por `id`, estilos extraídos de la maqueta aprobada, aviso
    de entrada e insignias.
  - **Verificado sirviendo por HTTP**, no abriendo archivos: el panel renderiza los archivos
    locales como instantánea y el CSS relativo no cargaba, lo que habría dado por buenas unas
    páginas rotas. Queda un `launch.json` para levantarlo cuando haga falta.
  - **`DC-03`:** el separador de las fuentes se escapaba a sí mismo. **Apareció mirando la
    página real en el navegador**, no leyendo el código — que es exactamente lo que la regla
    «verificar, no solo marcar» previene.
- **Dónde quedó:** 9/15 pasos. Falta el 3D (10-13), publicar (14) y medir (15).
- **Siguiente acción:** paso 10, `generar-geometria.py` para el `.glb` provisional.
- **Bloqueado por:** nada. El paso 14 necesita que Max active Pages.
- **Archivos modificados:** `herramientas/construir.py`, `src/` completo, `docs/` generado,
  `.claude/launch.json`, `motor/hitos/v0.1-cerebelo/03-bitacora-construccion.md`, `estado.md`.
