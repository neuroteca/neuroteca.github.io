
---

## S-14 — 2026-09-19 — Claude Opus 5

- **Carril / pieza / hito o lote / fase o paso:** Software — motor — `v0.1-cerebelo` — fase 3
- **Qué se hizo:**
  - **Excepción resuelta:** Max autorizó la descarga. Three.js `0.185.1` fijado en
    `src/vendor/`, 933 KB en 5 archivos, licencia MIT.
  - **Pasos 11, 12 y 13 completados.** El visor carga el `.glb`, gira, hace zoom, vuelve a la
    vista inicial, y al tocar un sector abre su ficha. Colorea por el eje principal y resalta
    el otro al apuntar a sus botones, con ratón y con teclado.
  - **`CA-A1` probado de verdad:** apagada la pieza en `src/piezas.json`, el sitio pasa de
    **1293 KB a 78 KB** y sigue completamente navegable. La pieza no deja residuo.
  - **`CA-A2` probado de verdad:** borrado el `.glb`, aparece el esquema SVG con el aviso «No
    se pudo cargar el modelo». Ficha y navegación intactas.
  - **`DC-06` — el fallo más instructivo del hito.** El SVG y el 3D se veían a la vez por dos
    causas encadenadas: `hidden` no existe como propiedad en un `SVGElement`, y aunque
    existiera, `.visor svg { display: block }` gana el empate de especificidad. **La segunda
    ya estaba escrita en las reglas promovidas de MIA que leí al diseñar este método, y aun
    así la pisé.** Se promueve a `reglas-del-agente.md` al cerrar.
  - **`DC-07`:** versión por contenido en los enlaces de CSS y JS. Perdí una vuelta entera
    diagnosticando un fallo ya corregido que el navegador servía en caché; publicado, le
    pasaría a cada visitante recurrente.
  - **`DC-08`:** la cámara se encuadra desde la caja del modelo. Con valores fijos, el modelo
    de Blender —que no tendrá estas dimensiones— quedaría fuera de plano, y `RT-05` dejaría
    de ser cierta.
  - **Falla de método detectada:** la primera comprobación de dependencias de Three.js solo
    miró la carpeta `jsm` y no el propio build, que importa `three.core.min.js`. Se repitió
    la comprobación sobre **todos** los archivos.
- **Dónde quedó:** 13/15 pasos. Sitio de 1293 KB, dentro del presupuesto.
- **Siguiente acción:** pasos 14 y 15, publicar y medir; después presentar la compuerta C.
- **Bloqueado por:** nada.
- **Archivos modificados:** `src/js/visor.js`, `src/piezas.json`, `src/vendor/` (nuevo),
  `src/estilos/sitio.css`, `src/plantillas/pagina.html`, `herramientas/construir.py`,
  `docs/`, `03-bitacora-construccion.md`, `estado.md`.
