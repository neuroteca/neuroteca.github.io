
---

## S-06 — 2026-09-18 — Claude Opus 5

- **Carril / pieza / hito o lote / fase o paso:** Software — motor — `v0.1-cerebelo` — fase 2
- **Qué se hizo:**
  - **Compuerta A aprobada por Max.** Alcance congelado. Los tres avisos aprobados.
  - **`DEC-A1` resuelta en la propia compuerta:** geometría aproximada generada por código,
    con Max modelando el definitivo en Blender en paralelo. Razones suyas: no bloquea el
    avance y le da tiempo de aprender la herramienta.
  - **Dos consecuencias derivadas y registradas**, que no estaban en la decisión pero se
    siguen de ella: (1) **la geometría se carga desde un archivo**, nunca escrita en el
    código, para que sustituir el modelo provisional sea un lote de contenido con compuerta
    V y no una reescritura de la pieza `visor`; (2) los activos declaran `estado`
    (`provisional` / `definitivo`) y **el visitante lo ve**, espejo del `borrador` /
    `revisado` de las fichas. Ambas en `esquema-contenido.md` v0.2.
  - **Fase 2 abierta en modo cuestionario.** `00-cuestionario-f2.md` escrito con cuatro
    preguntas: dirección de identidad visual (bloquea la compuerta B), primera vista, cómo
    se marca el modelo provisional, y listón de realismo de la geometría por código. El
    resto de la especificación lo cierra el agente y se aprueba de una pasada en B.
- **Dónde quedó:** cuestionario de fase 2 escrito, esperando respuestas de Max.
- **Siguiente acción:** Max responde el cuestionario; después se integra
  `02-especificacion.md` y se construye la maqueta de identidad visual.
- **Bloqueado por:** respuestas del cuestionario (la identidad visual bloquea la compuerta B).
- **Archivos modificados:** `motor/hitos/v0.1-cerebelo/01-diseno.md` (v1.1, compuerta A),
  `motor/hitos/v0.1-cerebelo/00-cuestionario-f2.md`, `motor/esquema-contenido.md` (v0.2),
  `estado.md`, `bitacora/`.
