
---

## S-16 — 2026-09-19 — Claude Opus 5

- **Carril / pieza / hito o lote / fase o paso:** Software — motor — `v0.1-cerebelo` — fase 3
- **Qué se hizo:**
  - **Defecto encontrado por Max al revisar la compuerta C**, con dos síntomas y una sola
    causa: cada estructura es una página HTML propia (`DEC-04`, decisión mía), así que navegar
    reconstruye la escena. De ahí (1) un destello del esquema de colores antes de cargar el
    modelo y (2) el reinicio del ángulo de la cámara.
  - **`DC-09`:** con la pieza activa, el esquema SVG nace oculto y solo vuelve si algo falla.
    Cuidado necesario: sin JavaScript no aparecería nunca, así que lleva un `<noscript>` que
    lo devuelve — si no, se romperían `RT-03` y `CA-A2`.
  - **`DC-10`:** la vista de la cámara se recuerda en `sessionStorage` y se restaura al cargar.
    Verificado: se gira el modelo, se cambia de ficha y el ángulo se conserva exacto.
    Descartado pasar a navegación en el cliente: contradiría `DEC-04`, de la que cuelgan
    `CA-05`, `CA-06` y `CA-A2` casi gratis.
  - **Observación para el lote de contenido:** girado de perfil, la aproximación se nota mucho
    —parece una lente, sin escotadura ni unión al tronco—. Coherente con lo aceptado en C-04,
    pero anotado como nota para el modelado definitivo.
- **Dónde quedó:** defecto corregido y verificado. Compuerta C sigue esperando a Max.
- **Siguiente acción:** Max vuelve a revisar y cruza la compuerta C.
- **Bloqueado por:** aprobación de la compuerta C.
- **Archivos modificados:** `src/js/visor.js`, `herramientas/construir.py`, `docs/`,
  `03-bitacora-construccion.md`, `estado.md`.
